# ECG-Monitor

This project implements a standalone IoT connected ECG monitor using an ESP32 as the microcontroller. The system features Ag/AgCl electrodes to acquire the cardiac signals and displays the waveform on an LCD. The data is processed and streamed to a smartphone dashboard using the Blynk platform. 

The system is powered by a 3.3
